#include <stdio.h>

int main () {
    int a;
    int array[5];
    int currentIndex = 0;
    while(scanf("%d", &a) == 1) {
        printf("Still working");
    }

    printf("Not Working");

    return 0;
    
}